name: CI

on:
  push:
      branches: [master]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2

      - name: Prepare
        run: |
          brew install make
          gmake --version
          gmake test
